/*
#ifndef HTTP_CONFIG_H
#define HTTP_CONFIG_H

// Reads saved config values from disk
int read_config(char* ip, size_t ip_len,
                char* user, size_t user_len,
                char* pass, size_t pass_len);

#endif
*/
